language: node_js
cache: npm
os: osx
node_js: '12'
install:
  - npm ci
  - npm run build
  - npm install -g codecov c8 hexo
  - c8 npm run test
  - c8 report --reporter=text-lcov > coverage.lcov
  - codecov
  - nvm install 10
  - npm install -g codecov c8 hexo
  - c8 npm run test
  - c8 report --reporter=text-lcov > coverage.lcov
  - codecov
